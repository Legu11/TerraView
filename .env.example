# Copier ce fichier en .env et adapter le mot de passe MySQL.
# Le .env est ignoré par git (il contient le mot de passe).

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=0000
DB_NAME=agriculture

API_HOST=127.0.0.1
API_PORT=5000
